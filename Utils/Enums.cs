
public enum Sides
{
    None,
    Left,
    Top,
    Right,
    Bottom,
    Vertical,
    Horizontal
}

public enum Scenes
{
    Menu,
    Game
}

public enum Layers
{
    None,
    Background,
    Shadows,
    Props
}